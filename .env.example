# Which DomainSources adapter serves the app. fixtures (default) needs nothing.
DATA_SOURCE=fixtures

# Inject failures into the fixture adapter to see error states in dev:
#   FIXTURE_FAULTS=members.list@glitch   → /glitch/members hits error.tsx
# Comma-separate several. Unknown operations fail fast at startup.
# FIXTURE_FAULTS=
